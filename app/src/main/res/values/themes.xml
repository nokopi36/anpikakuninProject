<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="AppTheme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FDFDFD</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_mozi</item>
        <item name="postSplashScreenTheme">@style/Theme.AnpikakuninProject</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.AnpikakuninProject" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/hcu_light_blue400</item>
        <item name="colorPrimaryVariant">@color/hcu_light_blue700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>